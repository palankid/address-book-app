/**
 * Application specific configuration
 * @module config/app
 */

/** Max users to be allowed to loaded and rendered */
export const MAX_USERS_COUNT = 90;

/** Next batch size of users */
export const PAGE_SIZE = 30;

/** Make sure we receive the same users from randomuser.me API */
export const SEED_NAME = 'addressbook';
